<div class="span10 offset1">
  <div class="well">
    <h2>Vítejte</h2>
    <p>On-line aplikace <strong>Kuchařka</strong> slouží ke sdílení a vyhledávání receptů. Jejím cílem je tyto činnosti co nejvíce zjednodušit svou přehledností. Díky funkci <strong>Lednička</strong> lze recepty vyhledávat i podle toho, jaké suroviny máte zrovna k dispozici.</p>
  </div>

  <div class="row-fluid">
    <div class="span4 well">
      <h3>Nejnovější recepty</h3>
      <% @newest.each do |item| %>
        <div class="spacer">
          <img src="<%=asset_path "minisandwich.png" %>" alt=""/>
          <b><%= item.name %></b>
          <p><%= item.annotation %></p>
          <p>Přidáno: <%= item.created_at %></p>
          <%= link_to "Zobrazit recept >>", recipe_path(item.id) %>
        </div>
      <% end %>
    </div>

    <div class="span4 well">
      <h3>Náhodné recepty</h3>
      <% @random.each do |item| %>
        <div class="spacer">
          <img src="<%=asset_path "minisandwich.png" %>" alt=""/>
          <b><%= item.name %></b>
          <p><%= item.annotation %></p>
          <%= link_to "Zobrazit recept >>", recipe_path(item.id) %>
        </div>
      <% end %>
    </div>

    <div class="span4 well">
      <h3>Nejlepší recepty</h3>
      <% @best.each do |item| %>
        <div class="spacer">
          <img src="<%=asset_path "minisandwich.png" %>" alt=""/>
          <b><%= item.name %></b>
          <p><%= item.annotation %></p>
          <p>Známka: <%= item.avg_value %></p>
          <%= link_to "Zobrazit recept >>", recipe_path(item.id) %>
        </div>
      <% end %>
    </div>
  </div>
</div>
